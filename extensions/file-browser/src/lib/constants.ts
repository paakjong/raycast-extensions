import { Color } from "@raycast/api";

export const FINDER_TAG_COLORS: Color[] = [
  Color.PrimaryText,
  Color.SecondaryText,
  Color.Green,
  Color.Purple,
  Color.Blue,
  Color.Yellow,
  Color.Red,
  Color.Orange,
];

export const EMOJI = {
  folder: "📁",
  file: "📄",
} as const;
